package dao;

import java.util.List;

import entity.User;

/**
 * 映射器:该接口中的方法对应的方法名时映射文件中那个唯一的id
 * @author HP
 *
 */
public interface UserDao {
	//查询所有用户数据
	//List<User> findAllUser();
}
